[pytest]
testpaths = tests
markers =
    slow: tests whose imports load the embedding model (~15s), e.g. the app or roster_store
# Endpoint tests are opt-in: `pytest` runs the fast suite, `pytest -m slow` or
# `pytest -m ""` includes the app-level ones.
addopts = -m "not slow"
