[pytest]
testpaths = tests
markers =
    slow: tests that import the full FastAPI app (loads the embedding model, ~15s)
# Endpoint tests are opt-in: `pytest` runs the fast suite, `pytest -m slow` or
# `pytest -m ""` includes the app-level ones.
addopts = -m "not slow"
